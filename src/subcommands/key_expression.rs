use crate::structs::{key_expression_config::KeyExpressionConfig, parsing_error::ParsingError};

pub fn key_expression(_input: String, _config: &KeyExpressionConfig) -> Result<(), ParsingError> {
    Ok(())
}
